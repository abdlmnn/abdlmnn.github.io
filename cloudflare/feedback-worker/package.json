{
  "name": "abdlmnn-feedback-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply abdlmnn_feedback --local",
    "db:migrate": "wrangler d1 migrations apply abdlmnn_feedback --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
